{% extends 'MainBundle::layout.html.twig' %}

{% block body -%}
    <h1>Student creation</h1>
    {{ form_start(form) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.lastname) }}
    {{ form_row(form.age) }}

    <div class="pull-right div-btn">
        {{ form_widget(form.submit)  }}
        <a href="{{ path('student') }}"> Retour</a>
    </div>
    {{ form_rest(form) }}
{% endblock %}
